<resources>

    <!-- EditTexts -->
    <style name="myAppTheme.TextViewLight" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:textSize">16sp</item>
        <item name="android:maxLines">1</item>
        <item name="android:textColor">@color/colorTextLight</item>
    </style>

    <!-- EditTexts -->
    <style name="AppTheme.EditTextDark" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorTextDark</item>
        <item name="colorPrimaryDark">@color/colorTextDark</item>
        <item name="colorAccent">@color/colorTextDark</item>

        <item name="colorControlNormal">@color/colorTextDark</item>
        <item name="colorControlActivated">@color/colorTextDark</item>
        <item name="colorControlHighlight">@color/colorTextDark</item>
    </style>

    <!-- Buttons -->
    <style name="AppTheme.ButtonsNormal" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorButtonNormal">@color/colorAccent</item>
        <item name="android:textColor">@color/colorTextVeryLight</item>
        <item name="android:textSize">28sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:paddingBottom">16dp</item>
        <item name="android:paddingTop">16dp</item>
        <item name="android:paddingRight">32dp</item>
        <item name="android:paddingLeft">32dp</item>
    </style>

    <!-- Keyboard -->


    <!-- Progress Bar -->


    <!-- Radio Buttons -->

    <!-- Toolbar -->

</resources>
